# HTTP API

OpenAPI spec: [`openapi.yaml`](openapi.yaml) (default `http://127.0.0.1:8787`).

Wire types live in `packages/contracts`. CORS, payload limits, and troubleshooting: [Local dev](local-dev.md).
