# Step 02 — TypeScript scaffold

## Objective

Add `adventure-llm/` package with strict TypeScript, Vitest, npm scripts, and README for env vars.

## Acceptance criteria

- [x] `package.json` with `test`, `build`, `check` scripts.  
- [x] `tsconfig.json` strict.  
- [x] Vitest configured.  
- [x] README documents `GEMINI_API_KEY` and local run instructions.

## Test strategy

`npm test` runs zero or placeholder test successfully; CI-ready.
